# jalankan di lokal

## step
- clone repo ini, atau download zip nya.

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`


## Edit member dan chat
1. buka file `constants.ts`. di sana bisa lihat ada const `MOCK_MESSAGES` untuk edit chat dan seterusnya.
